name: PHPCS Code Check

on: [push, pull_request]

jobs:
  phpcs-check:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'  # Asegura que es la versión de PHP adecuada para tu proyecto

    - name: Install PHPCS and Variable Analysis
      run: |
        composer require --dev "squizlabs/php_codesniffer=*"
        composer config allow-plugins.dealerdirect/phpcodesniffer-composer-installer true
        composer require --dev dealerdirect/phpcodesniffer-composer-installer
        composer require --dev sirbrillig/phpcs-variable-analysis

    - name: Check PHP Code Sniffer Rules
      run: |
        ./vendor/bin/phpcs -i

    - name: Run PHPCS with PSR12 and Variable Analysis
      run: |
        ./vendor/bin/phpcs malass_practicas.php --standard=PSR12 --runtime-set installed_paths vendor/sirbrillig/phpcs-variable-analysis
